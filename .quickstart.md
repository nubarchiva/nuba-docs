# Quickstart - nubarchiva-docs

## 🚀 Inicio Rápido

### Primera Vez

```bash
# 1. Ir al directorio
cd /Users/matutet/Documents/wrkspc.nubarchiva/nubarchiva-docs

# 2. Ejecutar el script (crea venv e instala dependencias automáticamente)
./serve.sh
```

### Uso Diario

```bash
cd /Users/matutet/Documents/wrkspc.nubarchiva/nubarchiva-docs
./serve.sh
```

Abre en navegador: http://127.0.0.1:8000

## 📝 Editar Documentación

1. Edita archivos `.md` en el directorio `docs/`
2. Los cambios se reflejan automáticamente en el navegador
3. Ctrl+C para detener el servidor

## 📂 Estructura

```
docs/
├── index.md              # Home
├── getting-started/      # Primeros pasos
├── user-guide/           # Guía usuario (búsqueda, docs, colecciones)
├── admin-guide/          # Guía admin (config, usuarios, backup)
├── customization/        # Personalización (genérica)
└── assets/               # Imágenes y videos
```

## 🔧 Comandos Útiles

### Activar/Desactivar Entorno Virtual (manual)

```bash
# Activar
source venv/bin/activate

# Desactivar
deactivate
```

### Build estático

```bash
source venv/bin/activate
mkdocs build
# Resultado en: site/
```

### Validar links

```bash
source venv/bin/activate
mkdocs build --strict
```

## 🎨 Elementos Markdown

### Notas Especiales

```markdown
!!! note "Nota"
    Información adicional

!!! tip "Consejo"
    Recomendación útil

!!! warning "Advertencia"
    Precaución

!!! danger "Importante"
    Información crítica

!!! example "Ejemplo"
    Caso práctico

??? question "¿Pregunta?"
    Respuesta colapsable
```

### Diagramas Mermaid

```markdown
\`\`\`mermaid
graph LR
    A[Inicio] --> B[Proceso]
    B --> C[Fin]
\`\`\`
```

### Imágenes

```markdown
![Alt text](../assets/images/nombre.png)
```

## 🐛 Troubleshooting

### Error: comando no encontrado

```bash
chmod +x serve.sh
./serve.sh
```

### Error: módulo no encontrado

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Puerto 8000 ocupado

```bash
mkdocs serve -a 127.0.0.1:8001
```

## 📦 Git

### Primer commit

```bash
git add .
git commit -m "feat: estructura inicial del manual de usuario nubarchiva"
```

### Después de editar

```bash
git add docs/
git commit -m "docs(user-guide): añadir sección de búsqueda avanzada"
git push
```

## 🌐 Deploy

GitHub Actions despliega automáticamente a GitHub Pages cuando haces push a `main`.

---

**Para documentación completa**: Ver [README.md](README.md)
